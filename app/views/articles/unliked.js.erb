$('<%= "#article_#{@article.id.to_s}" + " .like-element" %>').html("<%= escape_javascript(link_to 'like', liked_article_path(@article), remote: true, method: :post) %>")
$('<%= "#article_#{@article.id.to_s}" + " .like-count" %>').html("<%= pluralize(@article.users.count, 'like') %>")