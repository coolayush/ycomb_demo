<ul class = "no-bullets">
  <% @articles.each do |a| %>
    <li>
      <h3> <%= link_to a.title, a.url, target: "_blank" %> </h3>
      <p> <%= time_ago_in_words a.created_at %> ago</p> 
    </li>
  <% end%>
</ul>