<% if current_user %>
	$("#article-modal").html("<%= escape_javascript(render 'new') %>")
	$("#article-modal").modal("show")
<% else %>
  window.location.replace('<%= "http://#{request.host_with_port + log_in_path}" %>');
<% end %>